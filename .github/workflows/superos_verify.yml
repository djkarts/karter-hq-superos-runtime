name: SuperOS Runtime Verification

on:
  push:
    paths:
      - '.github/workflows/superos_verify.yml'
      - 'runtime/verify.sh'
  workflow_dispatch:

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Run verification script
        run: |
          chmod +x runtime/verify.sh
          ./runtime/verify.sh

